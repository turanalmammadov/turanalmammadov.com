# Neon (recommended on Vercel: Storage → Neon → connect to project).
# Vercel sets DATABASE_URL automatically after you link the database.
DATABASE_URL=

# Legacy name still supported if your provider only exposes this:
# POSTGRES_URL=
