# Neon — paste your pooled connection string (Vercel sets this automatically when Storage is connected).
DATABASE_URL=

# Also accepted if unset above: POSTGRES_URL, POSTGRES_PRISMA_URL, DATABASE_URL_UNPOOLED,
# POSTGRES_URL_NON_POOLING, or PGHOST+PGUSER+PGPASSWORD+PGDATABASE.
